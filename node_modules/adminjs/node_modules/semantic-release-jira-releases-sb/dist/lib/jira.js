"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const jira_connector_1 = tslib_1.__importDefault(require("jira-connector"));
function makeClient(config, context) {
    const basicAuth = context.env.JIRA_AUTH ? {
        base64: context.env.JIRA_AUTH,
    } : {
        email: context.env.JIRA_EMAIL,
        api_token: context.env.JIRA_TOKEN,
    };
    return new jira_connector_1.default({
        host: config.jiraHost,
        basic_auth: basicAuth,
    });
}
exports.makeClient = makeClient;
//# sourceMappingURL=jira.js.map